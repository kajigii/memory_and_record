<div class="side-bar-header">
  <div class="header-name">
    <%= link_to current_user.name, edit_user_path(current_user) %>
  </div>
  <div class="create-room">
    <%= link_to "新規作成", new_room_path %>
  </div>
</div>

<div class="rooms">
  <% current_user.rooms.each do |room| %>
    <div class="room">
      <div class="room-name">
        <%= link_to room.name, room_words_path(room) %>
      </div>
      <!-- 完成後にコメントアウト消す -->
      <%# <div class= "room-list">
        <%= link_to '削除', room_path(room), data: { turbo_method: :delete }, method: :delete %>
      <%# </div> %> 
    </div>
  <% end %>
  <% if @public_rooms.present? %>
    <% @public_rooms.each do |room| %>
      <div class="room">
        <div class="room-name">
          <%= link_to room.name, room_words_path(room) %>
        </div>
      </div>
    <% end %>
  <% else %>
  <% end %>
  <!-- 完成後にコメントアウト消す -->
      <%# <div class= "room-list">
        <%= link_to '削除', room_path(room), data: { turbo_method: :delete }, method: :delete %>
      <%# </div> %> 
</div>